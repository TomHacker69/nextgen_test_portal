version: "3.8"

services:
  # Shared Redis for Socket.IO Redis adapter & BullMQ Queue
  redis:
    image: redis:7-alpine
    container_name: nextgen-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

  # Code Execution Worker Service
  worker:
    build:
      context: ./frontend
      dockerfile: worker.Dockerfile
    container_name: nextgen-sandbox-worker
    restart: unless-stopped
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017/nextgen_test_portal
      - REDIS_URL=redis://redis:6379
      - NODE_ENV=production
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis

  # Web Application & Socket.IO Persistent Node Server
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nextgen-web-server
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017/nextgen_test_portal
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=super-secret-key-change-in-production-123456
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production
      - CORS_ORIGINS=http://localhost:3000,http://localhost:3001
      - NEXT_PUBLIC_SOCKET_URL=http://localhost:3000
      - NEXT_PUBLIC_API_URL=http://localhost:4000/api
    depends_on:
      - redis

  # Express API Server for Admin Panel
  api:
    build:
      context: ./frontend
      dockerfile: server/Dockerfile
    container_name: nextgen-api-server
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017/nextgen_test_portal
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=super-secret-key-change-in-production-123456
      - PORT=4000
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production
      - CORS_ORIGINS=http://localhost:3000,http://localhost:3001
    depends_on:
      - redis

volumes:
  redis-data:


